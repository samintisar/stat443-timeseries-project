# Project-level defaults for reproducible analysis.
options(
  repos = c(CRAN = "https://cloud.r-project.org"),
  stringsAsFactors = FALSE
)

# Auto-activate renv when available.
if (file.exists("renv/activate.R")) {
  source("renv/activate.R")
}
